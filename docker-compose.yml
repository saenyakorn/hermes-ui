version: '3.8'
services:
  hermes-agent:
    build: .
    ports:
      - "3000:3000"
    environment:
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=hermes_secret
      - PORT=3000
    volumes:
      - ./data:/data
